/**
 * The 2D integer vector used throughout the engine for positions, velocities and
 * accelerations. Grid coordinates use screen orientation: x grows to the right,
 * y grows downwards.
 *
 * (Type-only module: the engine does its vector arithmetic inline, which keeps
 * the bot's hot loops allocation-free.)
 *
 * @typedef {{ x: number, y: number }} Vec
 */

export {};
